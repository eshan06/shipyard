# Session handoff — 2026-06-15 (polish track)

Read this for what changed this session and **what I need from you**. Companion
docs: `PROGRESS.md`, `docs/RESUME_AFTER_MOVE.md`, `docs/RETURN_HANDOFF.md`,
`docs/ONBOARDING.md`, `legal/README.md`.

## TL;DR — status
1. **GitHub push — DONE.** `main` is pushed to **https://github.com/eshan06/shipyard**
   (private). The pushed history is **co-author-clean** and all gates are green.
   (The repo was created via the GitHub API once you injected the sandbox secret;
   the project is named *Shipyard*, so the repo is `shipyard`, not the earlier
   placeholder name.)
   > Only `main` was pushed (not `--tags`/`--mirror`). The local
   > `pre-coauthor-strip-backup` tag (and `refs/original/`) intentionally retain
   > the *old* co-authored history as a recovery point and were deliberately NOT
   > pushed. Once you're happy, delete them:
   > `git tag -d pre-coauthor-strip-backup && git update-ref -d refs/original/refs/heads/main`
2. **The legal pack needs your lawyer.** Everything in `legal/` is an unreviewed
   DRAFT, deliberately flagged. Do not publish any of it until counsel works
   through `legal/COUNSEL_REVIEW_CHECKLIST.md` (fills every `[[placeholder]]` and
   resolves every `⚠️ COUNSEL` callout). Start at `legal/README.md`.
3. **(Optional) Pick a real analytics sink.** Product analytics defaults to the
   `log` driver (durable structured events in the API logs — a real sink, nothing
   leaves your cluster). To route to a warehouse or PostHog, set `ANALYTICS_DRIVER`
   (see `.env.example`).

## What I did (all committed to `main`, gates green, no Claude co-author)
- **Post-move cleanup** (`7f94110`): reverted the OneDrive `.npmrc` workaround,
  pruned obsolete workaround scripts.
- **Product analytics — the `trackEvent` seam** (`0195baf`, `a15126d`):
  - API: a pluggable `app.analytics` sink with `ANALYTICS_DRIVER=log|http|posthog`
    (mirrors `DEPLOY_DRIVER`), and `POST /api/v1/telemetry` (auth-required; the
    server stamps the authenticated user so identity can't be spoofed).
  - Web: a buffering `trackEvent` client that batches to `/telemetry` (with
    `sendBeacon` on unload), an `AnalyticsProvider` (page views + flush), and
    instrumentation of sign-in/out, preview actions, and token create/revoke.
  - Note: the prompt referenced an "existing trackEvent seam" — it did **not**
    exist, so I built it.
- **First-run onboarding checklist** (`4aa4bfb`): a dismissible "Get started"
  card on the Overview page that derives steps (connect project → first preview →
  add a secret → create a token) from live data, plus `docs/ONBOARDING.md`.
- **Counsel-ready legal pack** (`fe4fa08`): ToS, AUP, Privacy, DPA (Annex I/II/III
  + SCC/CCPA), Subprocessors, Cookie, DMCA + README + counsel checklist — tailored
  to Shipyard (runs untrusted customer code; processor-vs-controller split).
- **Adversarial review + fixes** (`daa9385`): a 4-dimension subagent review (with
  per-finding verification) found 9 real issues; all fixed (onboarding event
  accuracy, beacon timestamp preservation, telemetry payload bounds, log
  redaction, PostHog key hardening).
- **Web test suite added** — `apps/web/vitest.config.ts` + 12 tests. The web
  `test` gate was previously red (no tests); it now passes.
- **History rewritten** to drop the `Co-Authored-By: Claude` trailer from all
  commits (you asked for no Claude co-author). Backup tag:
  `pre-coauthor-strip-backup`. Verified: 0 co-author trailers, trees identical.

## Build/verify note (important)
Moving off OneDrive did **not** fix the filesystem — the working tree is still a
slow, **symlink-hostile virtiofs** mount (`pnpm install` here fails with
`ERR_PNPM_EPERM symlink`). I build/verify on a fast-FS mirror at
`/home/agent/build/shipyard` (`bash /home/agent/sync.sh` then
`bash /home/agent/gate.sh all`). The committed `.npmrc` is intentionally the clean
default (correct for your real machine / CI / Docker). See `PROGRESS.md` build note.

## Verification status
`turbo typecheck` 11/11, `turbo build` 7/7, `turbo test` **191 passing** (api has 5
env-gated integration tests skipped by design). Full suite green after the review
fixes.

## Known follow-ups (non-blocking)
- The legal docs reference a `security.md` that doesn't exist yet — surfaced as the
  top `⚠️ COUNSEL` decision in `legal/README.md` (author one, or de-reference and
  rely on the DPA's Annex II). Not silently broken; flagged.
- Pre-existing backlog still open: slim Docker images, env-gate `trustProxy`,
  real Docker-driver e2e — see `docs/RESUME_AFTER_MOVE.md` §4.
